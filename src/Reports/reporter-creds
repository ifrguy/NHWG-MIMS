# Report generator credentials for MongoDB
# Add the credentials for the Mongo user that does reports
. ../getConfigValue
USERID=$(getConfigValue "reporter.user" "credentials.json")
PASSWD=$(getConfigValue "reporter.password" "credentials.json")
